# Interaction Rules

- Links: underline grows from inline-start on hover
- Opacity hover via `--opacity-hover`
- Focus: `--focus-ring` outline, always visible for keyboard
- No custom flashy cursor — native pointer only
- Buttons default to text + underline, not filled pills
