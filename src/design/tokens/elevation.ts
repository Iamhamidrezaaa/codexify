/**
 * Elevation — reserved for rare structural depth.
 * Decorative shadows are forbidden by art direction.
 */
export const elevation = {
  none: "none",
  subtle: "var(--elevation-subtle)",
} as const;
